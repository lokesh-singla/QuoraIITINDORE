from django.contrib import admin
from django.urls import path,include
from . import views

urlpatterns = [
    #path('', views.blogHome,name="blogHome"),
    #path('<int:id>', views.blogPost,name="blogPost"),
    #path('/edit/<int:id>', views.edit, name="categoryedit"),
]